# C# / .NET preflight

This is an environment check only. There is no coding task.

## Requirements

- .NET 8 SDK

## Test

```bash
dotnet test
```

Expected result: all tests pass.

## Run

```bash
dotnet run --project src/Preflight.Api
```

Then open:

```text
http://localhost:5000
```

You should see a simple **Preflight ready** page.

No code changes are required.
